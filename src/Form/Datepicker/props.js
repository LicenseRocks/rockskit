import PropTypes from "prop-types";

export const FormDatepickerPropTypes = {
  control: PropTypes.shape({}),
  defaultValue: PropTypes.shape({}),
  isRequired: PropTypes.bool,
  name: PropTypes.name,
};

export const FormDatepickerDefaultProps = {};
