import React from "react";

// UI
import { PageLoading } from ".";

export default {
  title: "PageLoading",
};

export const main = () => {
  const defaultProps = {};

  return <PageLoading {...defaultProps} />;
};
