import React from "react";

import { BaseTypography, BaseTypographyPropTypes } from "../Base";

export const H4 = (props) => {
  return <BaseTypography variant="h4" {...props} />;
};

H4.propTypes = BaseTypographyPropTypes;
